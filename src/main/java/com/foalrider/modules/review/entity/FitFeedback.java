package com.foalrider.modules.review.entity;

/**
 * Fit feedback enum for product sizing feedback.
 */
public enum FitFeedback {
    RUNS_SMALL,      // Product runs smaller than expected
    TRUE_TO_SIZE,    // Product fits as expected
    RUNS_LARGE       // Product runs larger than expected
}
